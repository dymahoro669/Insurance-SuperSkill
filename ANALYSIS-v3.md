# Insurance-SuperSkill 整合方案 v3.0

## 一、仓库全景概览

FDU-INS/Insurance-Skills 是由复旦大学许闲教授团队发起的面向保险场景的开源Skill基础设施平台，目前收录了 **539个Skills**，覆盖保险业务全链条。经过对全部539个skill逐一获取SKILL.md内容并分析，确认可以整合为覆盖保险全业务链的平台型超级Skill。

### 核心数据速览

| 指标 | 数据 |
|------|------|
| 原始Skills总数 | 539个 |
| 去重后独立能力模块 | ~430个 |
| 高质量（可直接复用） | ~230个（42.7%） |
| 中质量（需提炼整合） | ~195个（36.2%） |
| 低质量/空壳（淘汰） | ~95个（17.6%） |
| 不可访问 | ~19个（3.5%） |
| 重复组 | ~30组 |
| 非保险混入 | ~15-20个 |

---

## 二、总体架构 v3.0：中心化平台 + 分布式客户端

### 2.1 架构核心思路

上一版方案将所有组件（包括进化、评价、审计）都放在用户本地运行，存在三个根本性缺陷：

1. **平台空心化**：如果一切都运行在用户端，"中心经营的平台"就退化为一个下载站，失去了持续运营和价值沉淀的能力。
2. **进化不可靠**：要求用户每天凌晨保持设备在线来配合进化，在现实中完全不可行。
3. **评价不经济**：对每次调用都做LLM级别的全量评价，Token成本将远超实际使用成本。

v3.0采用 **"中心化平台（Hub）+ 轻量客户端（Spoke）"** 的轴辐式架构，明确划分平台侧职责和用户侧职责。

### 2.2 整体架构图

```
╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║                 Insurance-SuperSkill 中心平台（Hub）                   ║
║                 ─── 由平台方持续运营 ───                              ║
║                                                                      ║
║  ┌─────────────┐  ┌──────────────┐  ┌─────────────────────────┐    ║
║  │ Skill仓库    │  │ 进化引擎     │  │ Evolution Auditor       │    ║
║  │ & 版本管理   │  │ (每夜自动)   │  │ (整体测评, 一票否决)    │    ║
║  └──────┬──────┘  └──────┬───────┘  └───────────┬─────────────┘    ║
║         │                │                       │                  ║
║  ┌──────┴──────────────────────────────────────────┴──────────┐    ║
║  │                    全局基准 & 测试集                         │    ║
║  │  · 每子Skill ≥100标准用例  · 跨Skill协同用例  · 合规红线库  │    ║
║  └──────────────────────────┬─────────────────────────────────┘    ║
║                             │                                      ║
║  ┌──────────────────────────┴─────────────────────────────────┐    ║
║  │              聚合统计服务（匿名化遥测数据接收）               │    ║
║  │  · 接收: skill_id + 分数 + 耗时 + 状态码（不含任何用户数据）│    ║
║  │  · 输出: 全网质量趋势 · 进化优先级排序 · 能力缺口报告       │    ║
║  └────────────────────────────────────────────────────────────┘    ║
║                                                                      ║
║  ┌─────────────┐  ┌──────────────┐  ┌─────────────────────────┐    ║
║  │ 知识更新     │  │ 社区共建     │  │ 分发服务                │    ║
║  │ 管道         │  │ 审核通道     │  │ (版本推送/增量更新)     │    ║
║  └─────────────┘  └──────────────┘  └─────────────────────────┘    ║
║                                                                      ║
╚══════════════════════════════════╤═══════════════════════════════════╝
                                   │
                        HTTPS + 签名校验
                    (仅传输skill包+元数据
                     不收集任何用户数据)
                                   │
╔══════════════════════════════════▼═══════════════════════════════════╗
║                                                                      ║
║              用户端（Spoke）── 安装在用户本地设备                      ║
║                                                                      ║
║  ┌──────────────────────────────────────────────────────────────┐   ║
║  │              insurance-super-skill（主入口）                   │   ║
║  │  · 意图识别 → 领域路由 → 子skill调度 → 结果聚合              │   ║
║  │  · ┌─── 安全守卫层 (Security Guardian) ─── 全链路贯穿 ──┐    │   ║
║  │    │  输入脱敏 · 输出过滤 · 零采集 · 本地审计            │    │   ║
║  │    └────────────────────────────────────────────────────┘    │   ║
║  └───────────────────────────┬──────────────────────────────────┘   ║
║                              │                                      ║
║        ┌───┬───┬───┬───┬───┬┴─┬───┬───┬───┬───┬───┐               ║
║       核保│理赔│精算│合规│营销│保单│产品│客服│运营│风险│技术│知识     ║
║       (12个领域子Skills，仅接收脱敏后数据)                          ║
║                              │                                      ║
║  ┌───────────────────────────┴──────────────────────────────────┐   ║
║  │        Local Evaluator（轻量本地评价器，规则引擎，零Token）     │   ║
║  │  · 结构完整性检查  · 关键术语合规扫描  · 输出格式校验          │   ║
║  │  · 生成匿名化遥测摘要（可选上报）                              │   ║
║  └──────────────────────────────────────────────────────────────┘   ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝
```

### 2.3 平台侧 vs 用户侧职责划分

| 职责 | 平台侧（Hub） | 用户侧（Spoke） | 为什么这样分 |
|------|-------------|---------------|-------------|
| **Skill运行** | — | 12个领域子Skill + 安全守卫 | 业务处理贴近用户，保障响应速度和隐私 |
| **安全守卫** | 提供脱敏规则库更新 | 本地执行脱敏和过滤 | 规则由平台统一维护，执行在本地确保零泄露 |
| **质量评价** | 每日抽样深度评价（LLM-as-Judge） | 每次调用轻量规则评价（零Token） | 重评价集中做以降低成本，轻评价分散做以保覆盖面 |
| **自主进化** | 每夜由平台服务器执行 | 自动拉取更新后的Skill版本 | 进化需要算力+测试集+审计，必须由平台统一承担 |
| **进化审计** | Evolution Auditor运行在平台侧 | — | 需要完整测试集和全局视角，用户端不具备条件 |
| **版本分发** | 维护版本仓库，推送增量更新 | 自动或手动升级到最新版本 | 确保全网用户使用的始终是经审计通过的版本 |
| **知识更新** | 监控监管政策，更新知识库 | 接收知识库增量更新 | 法规监控需要持续运行的爬虫和专家审核团队 |
| **社区共建** | 提供贡献通道+审核流程+积分体系 | 提交反馈和建议 | 社区运营天然属于平台侧 |
| **遥测聚合** | 接收匿名化统计数据，生成全网报告 | 可选上报匿名化摘要（仅skill_id+分数+耗时） | 平台需要全局数据驱动进化，但绝不触碰原始数据 |

### 2.4 关键设计决策说明

**为什么进化不能放在用户端？**

上一版方案让进化在用户本地凌晨执行，这在实践中有三个致命问题：用户设备未必在线、用户端没有完整测试集（100个标准用例+60个全局冒烟用例）、用户端算力不足以支撑回归测试。正确的做法是进化由平台服务器在每夜统一执行，用户端只需在下次启动时自动拉取已审计通过的新版本即可——就像手机App的自动更新，用户完全无感。

**为什么评价要分层？**

如果每次调用都做LLM级别的全量评价（比如一天1000次调用 × 12个skill × 每次约2000 token），每天仅评价就需要消耗约2.4亿token，这在经济上完全不可行。分层评价的思路是：95%的调用用零成本的规则引擎做轻量评价，5%的调用由平台侧做深度LLM评价，Token成本降至原来的1/40。

**为什么版本管理权要交给用户？**

平台侧负责进化和审计，但用户对自己的环境拥有完全控制权。用户可以选择开启自动更新（推荐）、也可以锁定特定版本（生产环境常见需求）、还可以对主Skill和子Skill分别指定版本。这种"平台进化+用户决策"的模式既保证了持续改进，又尊重了用户对稳定性的要求。

---

## 三、安装机制（PowerShell通用命令）

### 3.1 一键安装命令

用户在PowerShell中执行以下命令即可完成全部安装：

```powershell
# 一键安装 Insurance-SuperSkill（含全部12个领域子Skill + 安全守卫 + 本地评价器）
powershell -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/dymahoro669/Insurance-SuperSkill/main/install.ps1 | iex"
```

或使用简写形式：

```powershell
# 等效简写
iwr -useb https://raw.githubusercontent.com/dymahoro669/Insurance-SuperSkill/main/install.ps1 | iex
```

### 3.2 安装脚本执行流程

install.ps1 脚本在用户设备上自动执行以下步骤（无任何前置依赖，开箱即用）：

```
[Step 1] 环境检测
  ├─ 检查 PowerShell 版本（≥5.1，兼容 Windows/macOS/Linux）
  ├─ 检查网络连通性（需访问 GitHub API）
  └─ 检查磁盘空间（需 ≥500MB）

[Step 2] 下载 Skill 包
  ├─ 从平台CDN下载 insurance-super-skill-1.0.0.zip（含全部15个组件）
  └─ SHA256 校验完整性 + 签名验证

[Step 3] 解压部署
  ├─ 解压到 ~/insurance-super-skill/（用户主目录下独立文件夹）
  ├─ 安装 ins-cli 命令行工具到用户 PATH
  ├─ 注册12个领域子Skill（自动关联到主索引）
  ├─ 部署安全守卫层配置
  └─ 部署本地评价器

[Step 4] 安全初始化
  ├─ 生成安全配置（脱敏规则引擎初始化）
  ├─ 创建本地审计日志目录
  ├─ 生成隐私偏好默认配置（遥测上报默认关闭）
  └─ 生成自动更新默认配置（默认开启）

[Step 5] 健康检查
  ├─ 对12个子Skill各运行3个冒烟测试用例
  ├─ 验证安全守卫脱敏引擎正常
  └─ 验证本地评价器正常

[Step 6] 输出安装报告 + ins-cli 使用说明
```

### 3.3 安装报告示例

```
==================================================
  Insurance-SuperSkill v1.0.0 安装完成
==================================================

  已安装组件: 15/15
  ├─ 主入口 (insurance-super-skill)     v1.0.0  ✓
  ├─ 领域Skills (12个)                  v1.0.0  ✓ 全部就绪
  │   核保 · 理赔 · 精算 · 合规 · 营销 · 保单
  │   产品 · 客服 · 运营 · 风险 · 技术 · 知识
  ├─ 安全守卫 (security-guardian)       v1.0.0  ✓ 已激活
  └─ 本地评价器 (local-evaluator)       v1.0.0  ✓ 已激活

  健康检查: 36/36 用例通过
  安全配置: 零采集模式 | 输入脱敏已开启 | 审计日志仅本地
  自动更新: 开启（可通过 ins-cli config auto-update off 关闭）
  遥测上报: 关闭（可通过 ins-cli config telemetry on 开启）
  安装位置: ~/insurance-super-skill/

  命令行工具 ins-cli 已就绪，常用命令：
    ins-cli status          查看当前版本和状态
    ins-cli update          检查并安装更新
    ins-cli config          管理配置项

  现在可以直接使用了！试试说：
  "帮我分析这份重疾险的理赔材料是否齐全"

==================================================
```

### 3.4 版本管理 CLI（ins-cli）

安装完成后，用户通过 `ins-cli` 命令行工具对本地Skill进行完整的版本控制。所有命令在PowerShell/CMD/Terminal中均可执行。

#### 3.4.1 状态查看

```powershell
# 查看整体状态和所有组件版本
ins-cli status

# 输出示例:
# ┌──────────────────────────────────────────────┐
# │  Insurance-SuperSkill                        │
# │  主入口:    v1.2.0  (最新)                   │
# │  自动更新:  ON                               │
# │                                              │
# │  领域Skills:                                 │
# │    ins-underwriting   v1.2.0  (最新)         │
# │    ins-claims         v1.1.3  (有更新 v1.2.0)│
# │    ins-actuarial      v1.2.0  (最新)         │
# │    ...                                       │
# │                                              │
# │  治理组件:                                   │
# │    security-guardian  v1.2.0  (最新)         │
# │    local-evaluator    v1.1.0  (最新)         │
# └──────────────────────────────────────────────┘

# 查看某个子Skill的详细信息
ins-cli status ins-claims
```

#### 3.4.2 自动更新开关

```powershell
# 关闭自动更新（安装后默认开启）
ins-cli config auto-update off

# 重新开启自动更新
ins-cli config auto-update on

# 设置自动更新策略
ins-cli config auto-update minor    # 仅自动更新小版本（如1.1→1.2），大版本手动确认
ins-cli config auto-update patch    # 仅自动更新补丁版本（如1.1.0→1.1.1），最保守
ins-cli config auto-update all      # 所有版本自动更新（默认）
```

#### 3.4.3 安装指定版本（主Skill整体）

```powershell
# 安装最新稳定版
ins-cli update --latest

# 安装指定版本（主Skill和所有子Skill一起回退/升级）
ins-cli install --version 1.1.0

# 查看可用的历史版本
ins-cli versions
# 输出:
#   v1.2.0  (current)  2026-05-30  PASS
#   v1.1.3             2026-05-15  PASS
#   v1.1.0             2026-04-20  PASS
#   v1.0.0             2026-03-01  PASS
```

#### 3.4.4 安装指定版本（单个子Skill）

```powershell
# 仅升级/回退某个子Skill到指定版本，不影响其他组件
ins-cli install ins-claims --version 1.2.0

# 将某个子Skill锁定在当前版本（即使自动更新开启，也不更新此Skill）
ins-cli pin ins-claims

# 解除锁定
ins-cli unpin ins-claims

# 查看哪些子Skill被锁定
ins-cli pinned
# 输出:
#   ins-claims         v1.1.3  PINNED (不会自动更新)
#   ins-underwriting   v1.2.0  (未锁定)
```

#### 3.4.5 检查更新与更新

```powershell
# 仅检查有哪些可用更新（不安装）
ins-cli update --check
# 输出:
#   可用更新:
#     ins-claims        v1.1.3 → v1.2.0  (未锁定)
#     ins-actuarial     v1.1.0 → v1.2.0  (未锁定)
#   已锁定（跳过）:
#     ins-compliance    v1.0.5 → v1.1.0  (PINNED)

# 安装所有可用更新（排除已锁定的）
ins-cli update

# 安装更新并显示详细变更日志
ins-cli update --verbose
# 输出:
#   ins-claims v1.1.3 → v1.2.0:
#     · 新增交通事故认定书检查项
#     · 优化材料缺失时的补件建议
#     · Evolution Auditor 审计: PASS (2026-05-29)
```

#### 3.4.6 配置管理一览

```powershell
# 查看所有配置项及当前值
ins-cli config --list
# 输出:
#   auto-update      on          # 自动更新开关 (on/off/minor/patch/all)
#   telemetry        off         # 匿名遥测上报 (on/off)
#   audit-log        local       # 审计日志存储 (local/off)
#   security-level   strict      # 安全守卫级别 (strict/standard/minimal)
#   language         zh-CN       # 默认输出语言
#   platform-url     https://github.com/dymahoro669/Insurance-SuperSkill  # 平台仓库

# 修改任意配置项
ins-cli config <key> <value>
```

#### 3.4.7 完整命令速查表

| 命令 | 作用 |
|------|------|
| `ins-cli status` | 查看所有组件版本和状态 |
| `ins-cli status <skill-id>` | 查看指定子Skill详情 |
| `ins-cli versions` | 查看可用的历史版本列表 |
| `ins-cli update --check` | 检查可用更新（不安装） |
| `ins-cli update` | 安装所有可用更新 |
| `ins-cli update --latest` | 安装最新稳定版 |
| `ins-cli install --version <ver>` | 安装指定版本（整体） |
| `ins-cli install <skill-id> --version <ver>` | 安装指定子Skill的指定版本 |
| `ins-cli pin <skill-id>` | 锁定子Skill版本（不参与自动更新） |
| `ins-cli unpin <skill-id>` | 解除子Skill版本锁定 |
| `ins-cli pinned` | 查看已锁定的子Skill列表 |
| `ins-cli config --list` | 查看所有配置项 |
| `ins-cli config <key> <value>` | 修改配置项 |
| `ins-cli audit --view` | 查看本地审计日志 |
| `ins-cli audit --purge` | 清除所有本地审计日志 |
| `ins-cli audit --export <path>` | 导出本地审计日志 |
| `ins-cli uninstall` | 完全卸载 Insurance-SuperSkill |

---

## 四、信息安全保障策略

### 4.1 零采集承诺

**Insurance-SuperSkill 严格遵守"零采集"原则：在任何使用场景下，都不会获取、存储、传输用户的任何敏感数据。** 所有业务数据处理均在用户本地完成，平台侧仅接收完全匿名化的统计摘要（且默认关闭）。

### 4.2 安全守卫层（运行在用户本地）

安全守卫嵌入主索引Skill内部，对所有12个子Skill的输入和输出进行全链路保护：

```
用户原始输入
    │
    ▼
[安全守卫] 输入脱敏引擎
  身份证号 → [ID_REDACT_1]     银行卡号 → [CARD_REDACT_1]
  手机号   → [PHONE_REDACT_1]  真实姓名 → [NAME_REDACT_1]
  详细地址 → [ADDR_REDACT_1]   保单号   → [POL_REDACT_1]
  病历信息 → [MED_REDACT_1]    邮箱     → [EMAIL_REDACT_1]
  社保号   → [SSN_REDACT_1]    护照号   → [PASSPORT_REDACT_1]
  车牌号   → [PLATE_REDACT_1]  房产信息 → [PROP_REDACT_1]
    │
    ▼  （此时已无敏感信息）
[主索引] 意图路由 → [子Skill] 业务处理
    │
    ▼
[安全守卫] 输出过滤引擎
  · 还原占位符为原始值
  · 二次扫描: 检测是否有意外PII泄露（子Skill推断出的信息）
  · 如检测到泄露 → 拦截并重新生成
    │
    ▼
用户看到的输出（敏感信息完整还原）
```

### 4.3 七层安全防护

| 层级 | 名称 | 运行位置 | 作用 |
|------|------|---------|------|
| 第1层 | 输入自动脱敏 | 用户本地 | 18类敏感信息在进入子Skill前替换为占位符 |
| 第2层 | 输出PII泄露检测 | 用户本地 | 二次扫描输出，拦截未预期的敏感信息 |
| 第3层 | 零采集承诺执行 | 用户本地 | 不采集/不存储/不传输用户输入、身份、对话历史 |
| 第4层 | 本地审计日志 | 用户本地 | 调用记录仅存本地，用户可查阅/导出/清除 |
| 第5层 | 子Skill沙箱隔离 | 用户本地 | 每个子Skill独立沙箱，仅可见脱敏后数据 |
| 第6层 | 安全声明合规 | 平台+用户 | 每个子Skill必须包含安全声明，平台侧审核 |
| 第7层 | 安全自检 | 用户本地 | 每次启动时验证防护机制正常 |

### 4.4 遥测数据（可选上报，默认关闭）

如果用户选择开启匿名遥测以帮助改进产品，上报的数据严格限定为：

```json
{
  "skill_id": "ins-claims",
  "version": "1.2.3",
  "local_eval_score": 87,
  "processing_ms": 3200,
  "status": "success",
  "timestamp_hour": "2026-05-31T14:00:00Z"
}
```

**绝不包含**：用户输入内容、输出内容、用户身份、对话历史、文件路径、设备信息。

---

## 五、分层评价体系（Token成本优化方案）

### 5.1 问题量化

如果对每次Skill调用都进行LLM级别的六维评价：

| 参数 | 数值 |
|------|------|
| 假设日调用量 | 1,000次（中等规模用户群） |
| 每次评价Token消耗 | ~2,000 tokens（输入+输出） |
| 日Token消耗 | 200万 tokens |
| 月Token消耗 | 6,000万 tokens |
| 月成本估算（按主流LLM定价） | 约 $180-360/月 |

**这对一个Skill评价体系而言成本过高。** 因此必须采用分层策略。

### 5.2 三层评价架构

```
┌────────────────────────────────────────────────────────────┐
│                    分层评价体系                              │
│                                                            │
│  ┌─── L1: 本地规则评价器（用户侧，每次调用，零Token）──┐   │
│  │                                                    │   │
│  │  覆盖率: 100% 的调用                               │   │
│  │  Token消耗: 0（纯规则引擎，不调用LLM）             │   │
│  │  延迟: < 50ms                                      │   │
│  │                                                    │   │
│  │  检查项:                                           │   │
│  │  ├─ 输出结构完整性（是否包含必要字段）             │   │
│  │  ├─ 关键保险术语规范性（术语词典匹配）             │   │
│  │  ├─ 输出长度合理性（不过短/过长）                  │   │
│  │  ├─ 合规红线扫描（关键词黑名单检查）                 │   │
│  │  ├─ 格式规范检查（JSON/表格/列表格式正确）         │   │
│  │  └─ 数值合理性（保费/赔付金额在合理范围内）        │   │
│  │                                                    │   │
│  │  输出: pass/warn/fail + 各检查项得分（0-100）      │   │
│  └────────────────────────────────────────────────────┘   │
│                         │                                  │
│                         │ 上报匿名摘要（可选）              │
│                         ▼                                  │
│  ┌─── L2: 平台抽样评价（平台侧，每日批量，低Token）──┐    │
│  │                                                   │    │
│  │  覆盖率: 5% 的调用（分层抽样）                     │    │
│  │  抽样策略:                                        │    │
│  │  ├─ L1评分低于60分的100%全量抽取（问题优先）      │    │
│  │  ├─ L1评分60-80分的20%抽取（边界关注）            │    │
│  │  ├─ L1评分80+的2%随机抽取（基线监控）             │    │
│  │  └─ 新用户首次调用100%抽取（冷启动关注）          │    │
│  │                                                   │    │
│  │  评价方式: 轻量级LLM-as-Judge                     │    │
│  │  · 使用小模型（如GPT-4o-mini级别）                 │    │
│  │  · 每次约800 tokens（精简Prompt）                  │    │
│  │  · 仅评价准确性和完整性两个核心维度                │    │
│  │                                                   │    │
│  │  日Token消耗: ~1,000次 × 5% × 800 = 40,000 tokens │    │
│  │  月成本: 约 $3-6/月                               │    │
│  └───────────────────────────────────────────────────┘    │
│                         │                                  │
│                         ▼                                  │
│  ┌─── L3: 深度评价（平台侧，每周/触发式，中Token）──┐    │
│  │                                                   │    │
│  │  覆盖率: < 1% 的调用                              │    │
│  │  触发条件:                                        │    │
│  │  ├─ L2发现系统性问题（同一子Skill连续3天低分）     │    │
│  │  ├─ 新版本发布后的首批调用                         │    │
│  │  ├─ 新场景/新险种首次出现                          │    │
│  │  └─ 每周例行深度评测（每子Skill抽10个代表性case）  │    │
│  │                                                   │    │
│  │  评价方式: 完整六维LLM评价                         │    │
│  │  · 使用强模型（GPT-4o/Claude级别）                 │    │
│  │  · 每次约3,000 tokens                              │    │
│  │  · 含详细findings和improvement suggestions         │    │
│  │                                                   │    │
│  │  周Token消耗: ~12×10×3000 + 触发 ≈ 50,000 tokens  │    │
│  │  月成本: 约 $5-10/月                              │    │
│  └───────────────────────────────────────────────────┘    │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 5.3 Token成本对比

| 方案 | 月Token消耗 | 月成本估算 | 相比全量LLM评价 |
|------|------------|-----------|----------------|
| 全量LLM评价（v2.0方案） | 6,000万 | $180-360 | 100% |
| **分层评价（v3.0方案）** | **~270万** | **$8-16** | **~4.5%** |
| 纯规则评价（无LLM） | 0 | $0 | 0%（但质量不可控） |

**分层方案在保持质量监控覆盖面的同时，将Token成本降低到全量方案的约1/20。**

### 5.4 评价数据驱动进化的流程

```
每日运行:
  L1（用户侧）: 1,000次调用 → 1,000条规则评分记录 → 生成匿名摘要
  L2（平台侧）: 50条抽样 → 轻量LLM评价 → 识别问题模式

每周运行:
  L3（平台侧）: 120条深度评测 → 完整六维报告 → 生成进化建议

每日凌晨进化决策:
  聚合 L1统计 + L2发现 + L3报告
    │
    ▼
  进化优先级排序:
    · 合规性失分 → 最高优先级（立即修复）
    · L2连续低分的子Skill → 高优先级
    · L3发现系统性问题 → 中优先级
    · L1统计中结构性趋势变化 → 低优先级（观察）
    │
    ▼
  选择Top 2子Skill进入今夜进化流程
```

---

## 六、中心化平台：每夜自主进化与整体审计

### 6.1 进化引擎（运行在平台服务器）

进化引擎是平台侧的**常驻服务**，每夜自动运行，不依赖任何用户设备。

```
平台服务器（每夜 02:00 CST 自动启动）
    │
    ▼
[Step 1] 收集进化素材
  ├─ 读取当日 L1聚合统计（匿名化）
  ├─ 读取当日 L2评价发现
  ├─ 读取本周 L3深度报告
  ├─ 读取社区反馈和贡献
  └─ 读取监管政策变更通知
    │
    ▼
[Step 2] 进化优先级排序
  ├─ 合规性问题 → P0（必须今夜修复）
  ├─ 连续3天低分的子Skill → P1
  ├─ 新场景/新险种无法处理 → P2
  └─ 渐进优化（提升可读性/完整性）→ P3
    │
    ▼
[Step 3] 生成进化方案（每夜最多修改2个子Skill）
  ├─ 知识补丁: 补充缺失的保险知识点
  ├─ Prompt优化: 改进低分维度的指令表达
  ├─ 流程调整: 修复工作流中的瓶颈或遗漏
  └─ 规则更新: 更新过时的法规/产品信息
    │
    ▼
[Step 4] 沙箱验证
  ├─ 在平台侧隔离环境中应用变更
  ├─ 运行被修改子Skill的完整测试集（≥100个标准用例）
  ├─ 运行关联子Skill的协同测试集（≥30个跨Skill用例）
  └─ 运行全局冒烟测试（12个子Skill各5个核心用例 = 60个用例）
    │
    ▼
[Step 5] 生成变更报告
  ├─ 修改了什么（精确到具体知识点/规则/Prompt段落）
  ├─ 为什么修改（关联到具体评价发现）
  └─ 预期效果（预估对哪些维度有提升）
    │
    ▼
[Step 6] 提交 Evolution Auditor 进行整体测评
    │
    ├─ PASS → 发布新版本号 → 推送到分发服务
    ├─ CONDITIONAL_PASS → 发布 + 标记待人工复核
    └─ FAIL → 回滚 → 冻结该子Skill自动进化 → 通知运维团队
```

### 6.2 Evolution Auditor（运行在平台服务器）

Evolution Auditor 是独立于进化引擎的审计组件，在每次进化后对Skill体系进行全方位整体测评。

**六维整体测评框架**：

| 维度 | 代号 | 测评内容 | 最低通过线 |
|------|------|---------|-----------|
| **完整性 (Integrity)** | INT | 进化后是否保留所有原有能力？回归测试是否全部通过？ | 95分 |
| **可用性 (Usability)** | USA | 输出是否对用户友好？交互流程是否顺畅？错误处理是否完善？ | 80分 |
| **自洽性 (Consistency)** | CON | 内部逻辑是否自洽？跨Skill接口是否一致？路由是否正确？ | 90分 |
| **高效性 (Efficiency)** | EFF | 响应时间/Token消耗是否未恶化？路由跳数是否合理？ | 80分 |
| **专业性 (Professionalism)** | PRO | 保险知识是否准确？术语是否规范？法规是否最新？ | 90分 |
| **可读性 (Readability)** | REA | SKILL.md文档是否清晰？变更说明是否易理解？ | 80分 |

**所有维度必须同时达标，任何一维不通过则整个进化回滚——一票否决。**

测评流程：

```
接收变更报告
    │
    ├─ Phase 1: 回归测试（INT）
    │   · 被修改Skill完整测试集（≥100用例）
    │   · 关联Skill协同测试集（≥30用例）
    │   · 全局冒烟测试（60用例）
    │
    ├─ Phase 2: 一致性审计（CON）
    │   · 接口schema兼容性检查
    │   · 术语一致性扫描
    │   · 路由表正确性验证
    │   · 安全守卫层正常工作验证
    │
    ├─ Phase 3: 性能基准对比（EFF）
    │   · 10个标准请求耗时 vs 进化前
    │   · Token消耗量对比
    │   · 路由跳数检查
    │
    ├─ Phase 4: 专业审查（PRO）
    │   · 新增知识点事实核查
    │   · 法规/产品时效性检查
    │   · 计算类功能数值验证
    │
    ├─ Phase 5: 用户视角测试（USA + REA）
    │   · 模拟5个典型用户场景
    │   · 输出可读性和可操作性评估
    │   · 错误场景降级表现
    │
    └─ Phase 6: 综合评定
        · 六维得分
        · 审计结论: PASS / FAIL / CONDITIONAL_PASS
        · FAIL → 自动生成回滚指令
```

### 6.3 进化安全网

```
┌─── 五重安全网 ────────────────────────────────────┐
│                                                   │
│  规则1: 单次进化范围限制                            │
│  · 每夜最多修改2个子Skill                          │
│  · 每个子Skill每夜最多修改3个知识点/规则            │
│  · 超出范围自动拆分到多夜执行                       │
│                                                   │
│  规则2: 版本回滚机制                                │
│  · 平台保留最近30天的完整Skill快照                  │
│  · Auditor判定FAIL → 自动回滚到上一版本             │
│  · 连续3次进化失败 → 冻结+人工介入                  │
│                                                   │
│  规则3: 全局一致性校验                              │
│  · 每次进化后运行全局冒烟测试（60个核心用例）       │
│  · 任何子Skill冒烟失败 → 整个进化批次回滚           │
│                                                   │
│  规则4: 跨Skill接口契约保护                         │
│  · 维护"跨Skill协同契约"清单                       │
│  · 进化不得改变契约中声明的接口schema               │
│  · 如需修改 → 必须同时更新所有依赖方                │
│                                                   │
│  规则5: 进化节奏控制                                │
│  · 每夜最多1次自动进化                              │
│  · 紧急修复（合规P0）需额外安全审查                 │
│  · 每月生成进化月报                                │
│                                                   │
└───────────────────────────────────────────────────┘
```

### 6.4 用户端如何获得进化成果

用户完全不需要配合进化过程。进化成果通过以下方式到达用户端：

```
平台侧进化完成并通过审计
    │
    ▼
新版本发布到分发服务（GitHub Releases）: https://github.com/dymahoro669/Insurance-SuperSkill/releases
    │
    ▼
用户端获取更新:
  ├─ [自动模式] auto-update 开启时:
  │   ├─ ins-cli 后台定时检查版本差异（每6小时一次）
  │   ├─ 自动下载增量更新包（通常 <100KB）
  │   ├─ SHA256校验 + 签名验证
  │   └─ 下次调用时自动使用新版本
  │       │
  │       └─ 更新通知（非阻断式）:
  │          "Insurance-SuperSkill 已更新到 v1.0.3
  │           · 理赔材料检查新增交通事故认定书检查项
  │           · 核保问卷分析优化了职业风险评估"
  │
  └─ [手动模式] auto-update 关闭时:
      ├─ ins-cli update --check 提示有可用更新
      └─ 用户自行决定何时执行 ins-cli update
```

---

## 七、主索引路由设计

### 7.1 意图识别与路由规则

```yaml
路由映射表:
  核保: { 触发词: [投保, 核保, 告知, 健康问卷, 风险评级, 加费, 除外, 拒保, 承保] → ins-underwriting }
  理赔: { 触发词: [理赔, 报案, 索赔, 拒赔, 申诉, 材料, 定损, 赔付, 出险] → ins-claims }
  精算: { 触发词: [定价, 准备金, IBNR, 精算, 费率, 损失率, 资本模型, IFRS17] → ins-actuarial }
  合规: { 触发词: [合规, 审计, HIPAA, 反洗钱, AML, 监管, 法规变化] → ins-compliance }
  营销: { 触发词: [推荐, 销售, 获客, 转化, 报价, 营销方案, 交叉销售] → ins-marketing }
  保单: { 触发词: [保单, 续保, 保全, 批改, 条款解读, 覆盖范围] → ins-policy }
  产品: { 触发词: [产品介绍, 险种, 保障, 比较, 哪款好, 规划] → ins-product }
  客服: { 触发词: [话术, FAQ, 投诉, 客户沟通, 信函] → ins-service }
  运营: { 触发词: [KPI, 报告, 自动化, 工作流, 团队, 培训] → ins-ops }
  风险: { 触发词: [风险评估, 巨灾, 气候风险, 网络风险, 风险登记] → ins-risk }
  技术: { 触发词: [API, Guidewire, 数据处理, 系统集成, 表单, 标准] → ins-tech }
  知识: { 触发词: [什么是, 社保, 医保, 法规, 术语, 基础知识] → ins-knowledge }

跨Skill协同:
  "这个客户的重疾险理赔被拒了，帮我分析原因并准备申诉"
  → Claims(理赔分析) → Underwriting(核保结论复核) → Compliance(合规检查)
```

### 7.2 降级策略

意图不明确时：1) 询问用户澄清 → 2) 路由到ins-knowledge（兜底）→ 3) 通用建议+推荐方向。

---

## 八、十二大领域子Skill设计

### 8.1 智能核保 (ins-underwriting)

整合~15个精华skill。投保问卷三阶段分析（初审→复核→终审）、健康告知校验、6大险种深度核保（意外/年金/健康/寿险/医疗/重疾）、地理风险查询、核保结论生成与解释。

### 8.2 智能理赔 (ins-claims)

整合~20个精华skill。理赔材料智能审核（按险种匹配）、条款自动定位、案件时间线重建、保障范围判断、高风险八维度预警、申诉信+对抗性模拟、定损估值。

### 8.3 精算分析 (ins-actuarial)

整合~12个精华skill。三大精算支柱（定价/准备金/风险管理）、精算建模（7阶段审查）、财务分析、IFRS 17合规、巨灾建模、94个专业计算工具。

### 8.4 合规风控 (ins-compliance)

整合~15个精华skill。合规审计（HIPAA/COI/AML）、监管变化分析、反欺诈筛查、EU AI Act评估、网络安全合规。

### 8.5 营销推荐 (ins-marketing)

整合~10个精华skill。客户需求分析、产品匹配推荐、竞品比较、营销方案撰写、交叉销售、报价单生成。

### 8.6 保单服务 (ins-policy)

整合~10个精华skill。保单条款解读、多保单比较、续保策略、覆盖充分性评估、保单QA。

### 8.7 产品咨询 (ins-product)

整合~12个精华skill。全险种咨询、产品设计与评审、需求Gap分析、市场趋势、个人/家庭/企业规划。

### 8.8 客服支持 (ins-service)

整合~8个精华skill。标准话术库、理赔沟通、流程引导、信函生成。

### 8.9 运营支持 (ins-ops)

整合~12个精华skill。运营自动化（含ROI）、KPI看板、团队培训、供应商管理、数据验证。

### 8.10 风险管理 (ins-risk)

整合~10个精华skill。企业风险评估、巨灾/气候/网络建模、地理空间分析、风险登记册。

### 8.11 技术工具 (ins-tech)

整合~10个精华skill。Guidewire集成、数据处理、合成测试数据、PDF表单、标准映射。

### 8.12 知识百科 (ins-knowledge)

整合~10个精华skill。各国社保/医保、保险百科、法规解读、术语标准化。

---

## 九、完整生态闭环

```
                    ┌── 用户日常使用 ──┐
                    │                 │
                    ▼                 │
          主索引路由 → 子Skill处理    │
          (安全守卫全程保护)           │
                    │                 │
                    ▼                 │
          L1本地规则评价(零Token)     │
                    │                 │
              匿名摘要(可选上报)       │
                    │                 │
                    ▼                 │
  ┌─── 平台侧（每夜）───────────────────────────────┐
  │                                                │
  │  聚合 L1统计 + L2抽样评价 + L3深度报告          │
  │         │                                      │
  │         ▼                                      │
  │  进化优先级排序 → 选择Top 2子Skill              │
  │         │                                      │
  │         ▼                                      │
  │  生成进化方案 → 沙箱验证                        │
  │         │                                      │
  │         ▼                                      │
  │  Evolution Auditor 六维整体测评                 │
  │         │                                      │
  │    ┌────┼────┐                                 │
  │    │         │                                 │
  │  PASS      FAIL                               │
  │    │         │                                 │
  │  发布新版本  回滚+冻结                          │
  │    │                                           │
  └────┼───────────────────────────────────────────┘
       │
       ▼
  分发服务推送更新
       │
       ▼
  用户端自动拉取新版本（无感更新）
       │
       └──→ 下次调用使用新版本 ──┘
```

---

## 十、实施路线图

### Phase 1: 核心整合与平台搭建（第1-3个月）

- 淘汰低质量/重复skills，提炼12个子skill核心内容
- 搭建主索引路由逻辑 + 安全守卫层
- 开发PowerShell一键安装脚本
- 搭建平台侧基础设施（版本管理、分发服务、测试集）
- 实现L1本地规则评价器

### Phase 2: 评价与进化体系（第4-5个月）

- 建立每子Skill ≥100个标准评测用例
- 开发L2平台抽样评价管道 + L3深度评价流程
- 开发进化引擎（每夜自动运行）
- 开发Evolution Auditor（六维整体测评+回归测试）
- 实现五重安全网

### Phase 3: 生态闭环（第6-7个月）

- 运行1个月真实进化周期，验证机制有效性
- 调优分层评价参数（L2抽样比例、L3触发阈值）
- 上线匿名遥测聚合服务
- 启动社区共建通道
- 发布v1.0正式版

### Phase 4: 持续运营（第8个月起）

- 每日自主进化稳定运行
- 每月发布进化月报
- 持续扩展新险种/新地域
- 沉淀复合技能包

---

## 十一、总结

### v3.0 核心特征

| 维度 | v2.0（上一版） | v3.0（本版） |
|------|--------------|-------------|
| **架构模型** | 完全去中心化（星型），所有组件在用户本地 | 轴辐式：中心化平台(Hub) + 轻量客户端(Spoke) |
| **平台角色** | 仅作为下载站，无持续运营能力 | 承担进化引擎、审计服务、版本分发、知识更新、社区运营 |
| **安装方式** | 伪代码命令，依赖特定平台 | PowerShell一行命令，无任何前置依赖，开箱即用 |
| **版本管理** | 无用户控制 | ins-cli完整CLI：自动更新开关、版本锁定、指定版本安装 |
| **进化执行位置** | 用户本地（需凌晨在线） | 平台服务器（7×24常驻，用户无感） |
| **评价Token成本** | 全量LLM评价，月成本$180-360 | 分层评价（L1规则+L2抽样+L3深度），月成本$8-16 |
| **用户配合要求** | 需要保持设备在线配合进化 | 零配合；用户通过ins-cli自主决定更新策略 |

### 组件清单

| 运行位置 | 组件 | 数量 | 职责 |
|---------|------|------|------|
| 用户本地 | 主入口（含路由+安全守卫） | 1 | 意图路由、安全防护 |
| 用户本地 | 领域子Skills | 12 | 保险业务处理 |
| 用户本地 | 本地评价器（L1） | 1 | 规则引擎评价，零Token |
| **用户侧合计** | | **14** | **一键安装，全自动** |
| 平台侧 | 进化引擎 | 1 | 每夜自动进化 |
| 平台侧 | Evolution Auditor | 1 | 整体测评，一票否决 |
| 平台侧 | L2/L3评价服务 | 1 | 抽样+深度评价 |
| 平台侧 | 版本分发服务 | 1 | 推送更新 |
| 平台侧 | 知识更新管道 | 1 | 法规/产品监控 |
| 平台侧 | 社区共建平台 | 1 | 贡献+审核+积分 |
| **平台侧合计** | | **6** | **持续运营** |

### 一句话总结

**v3.0采用"中心化平台+轻量客户端"的轴辐式架构：用户一条PowerShell命令即可安装全部能力（无任何前置依赖，开箱即用），通过ins-cli命令行工具自主控制自动更新开关、版本锁定和指定版本安装；所有业务处理在本地完成且受七层安全防护保护；平台侧（GitHub: dymahoro669/Insurance-SuperSkill）承担每夜自主进化、整体审计、版本分发和持续运营；分层评价体系（L1零成本规则+L2低成本抽样+L3按需深度）将Token消耗降至全量方案的1/20。**
